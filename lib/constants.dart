
import 'package:flutter/material.dart';

const kRoundRadiusContainer = BorderRadius.only(
                  topLeft: Radius.circular(25.0),
                  topRight: Radius.circular(25.0));

const kStyleHeader = TextStyle(
  color: Colors.white,
  fontSize: 35.0,
  fontWeight: FontWeight.w700,
);
const kStyleHeader1 = TextStyle(
  color: Colors.white,
);

const kBoxDecorationList = BoxDecoration(
    color: Colors.white,
    borderRadius: kRoundRadiusContainer
);